// Compat surface: openclaw/plugin-sdk/session-store-runtime
// Re-exports stable session store helpers for channel extensions.

export { readSessionUpdatedAt } from "../config/sessions/store.js";
export { resolveStorePath } from "../config/sessions/paths.js";
