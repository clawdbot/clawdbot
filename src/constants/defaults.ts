/**
 * Default host for local services
 */
export const DEFAULT_HOST = "127.0.0.1";

/**
 * Default gateway port
 */
export const DEFAULT_GATEWAY_PORT = 19001;

/**
 * Default localhost URL for gateway
 */
export const DEFAULT_GATEWAY_URL = `ws://${DEFAULT_HOST}:${DEFAULT_GATEWAY_PORT}`;

/**
 * Default callback host for OAuth
 */
export const DEFAULT_CALLBACK_HOST = DEFAULT_HOST;
