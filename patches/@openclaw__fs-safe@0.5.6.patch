diff --git a/dist/archive-deadline.js b/dist/archive-deadline.js
index a467a07..4b3ea6c 100644
--- a/dist/archive-deadline.js
+++ b/dist/archive-deadline.js
@@ -18,17 +18,26 @@ export async function waitForDeadline(promise, deadline) {
     if (deadline.signal.aborted) {
         throw deadlineReason(deadline);
     }
-    return await Promise.race([
-        promise,
-        new Promise((_, reject) => {
-            const abort = () => reject(deadlineReason(deadline));
-            deadline.signal.addEventListener("abort", abort, { once: true });
-            const cleanup = () => {
-                deadline.signal.removeEventListener("abort", abort);
-            };
-            promise.then(cleanup, cleanup);
-        }),
-    ]);
+    try {
+        return await Promise.race([
+            promise,
+            new Promise((_, reject) => {
+                const abort = () => reject(deadlineReason(deadline));
+                deadline.signal.addEventListener("abort", abort, { once: true });
+                const cleanup = () => {
+                    deadline.signal.removeEventListener("abort", abort);
+                };
+                promise.then(cleanup, cleanup);
+            }),
+        ]);
+    }
+    catch (error) {
+        if (!deadline.signal.aborted) {
+            throw error;
+        }
+        await promise.catch(() => undefined);
+        throw deadlineReason(deadline);
+    }
 }
 function createExtractionDeadline(timeoutMs, label) {
     const controller = new AbortController();
diff --git a/dist/archive-native.js b/dist/archive-native.js
index b7e1573..34be530 100644
--- a/dist/archive-native.js
+++ b/dist/archive-native.js
@@ -111,6 +111,7 @@ export async function extractNativeArchive(params) {
                     sourceDir: stagingDir,
                     destinationDir: params.destDir,
                     destinationRealDir,
+                    signal: params.deadline.signal,
                 });
             },
         });
diff --git a/dist/archive-staging.d.ts b/dist/archive-staging.d.ts
index 58bd6a0..4ac7e64 100644
--- a/dist/archive-staging.d.ts
+++ b/dist/archive-staging.d.ts
@@ -18,6 +18,7 @@ export declare function mergeExtractedTreeIntoDestination(params: {
     sourceDir: string;
     destinationDir: string;
     destinationRealDir: string;
+    signal?: AbortSignal;
 }): Promise<void>;
 export declare function createArchiveSymlinkTraversalError(originalPath: string): ArchiveSecurityError;
 //# sourceMappingURL=archive-staging.d.ts.map
diff --git a/dist/archive-staging.js b/dist/archive-staging.js
index 011724b..e8c7f45 100644
--- a/dist/archive-staging.js
+++ b/dist/archive-staging.js
@@ -12,6 +12,13 @@ import { getFsSafeTestHooks } from "./test-hooks.js";
 const ERROR_ARCHIVE_ENTRY_TRAVERSES_SYMLINK = "archive entry traverses symlink in destination";
 const ARCHIVE_STAGING_MODE = 0o700;
 export { ArchiveSecurityError } from "./archive-errors.js";
+function throwIfAborted(signal) {
+    if (!signal?.aborted) {
+        return;
+    }
+    const reason = signal.reason;
+    throw reason instanceof Error ? reason : new Error(String(reason));
+}
 function symlinkTraversalError(originalPath) {
     return new ArchiveSecurityError("destination-symlink-traversal", `${ERROR_ARCHIVE_ENTRY_TRAVERSES_SYMLINK}: ${formatErrorDetail(originalPath)}`);
 }
@@ -268,23 +275,33 @@ export async function withStagedArchiveDestination(params) {
     }
 }
 export async function mergeExtractedTreeIntoDestination(params) {
+    throwIfAborted(params.signal);
     const targetRoot = await root(params.destinationRealDir);
+    throwIfAborted(params.signal);
     const sourceRootGuard = await createDirectoryIdentityGuard(params.sourceDir);
+    throwIfAborted(params.signal);
     const sourceRootReal = sourceRootGuard.realPath;
     const walk = async (currentSourceDir) => {
+        throwIfAborted(params.signal);
         await assertDirectoryIdentityGuard(sourceRootGuard);
+        throwIfAborted(params.signal);
         const entries = await fs.readdir(currentSourceDir, { withFileTypes: true });
+        throwIfAborted(params.signal);
         for (const entry of entries) {
+            throwIfAborted(params.signal);
             await assertDirectoryIdentityGuard(sourceRootGuard);
+            throwIfAborted(params.signal);
             const sourcePath = path.join(currentSourceDir, entry.name);
             const relPath = path.relative(params.sourceDir, sourcePath);
             const originalPath = relPath.split(path.sep).join("/");
             const destinationPath = path.join(params.destinationDir, relPath);
             const sourceStat = await fs.lstat(sourcePath);
+            throwIfAborted(params.signal);
             if (sourceStat.isSymbolicLink()) {
                 throw symlinkTraversalError(originalPath);
             }
             const sourceReal = await fs.realpath(sourcePath);
+            throwIfAborted(params.signal);
             if (!isPathInside(sourceRootReal, sourceReal)) {
                 throw symlinkTraversalError(originalPath);
             }
@@ -297,13 +314,16 @@ export async function mergeExtractedTreeIntoDestination(params) {
                     originalPath,
                     isDirectory: true,
                 });
+                throwIfAborted(params.signal);
                 await walk(sourcePath);
+                throwIfAborted(params.signal);
                 await applyStagedEntryMode({
                     destinationRealDir: params.destinationRealDir,
                     relPath,
                     mode: sourceStat.mode & 0o777,
                     originalPath,
                 });
+                throwIfAborted(params.signal);
                 continue;
             }
             if (!sourceStat.isFile()) {
@@ -317,19 +337,23 @@ export async function mergeExtractedTreeIntoDestination(params) {
                 originalPath,
                 isDirectory: false,
             });
+            throwIfAborted(params.signal);
             try {
                 await targetRoot.copyIn(relPath, sourcePath, { mkdir: true });
+                throwIfAborted(params.signal);
                 await assertExtractedFileHasNoHardlinkAlias({
                     destinationRealDir: params.destinationRealDir,
                     relPath,
                     originalPath,
                 });
+                throwIfAborted(params.signal);
                 await applyStagedEntryMode({
                     destinationRealDir: params.destinationRealDir,
                     relPath,
                     mode: sourceStat.mode & 0o777,
                     originalPath,
                 });
+                throwIfAborted(params.signal);
             }
             catch (err) {
                 await removeExtractedDestinationFile({
@@ -344,6 +368,7 @@ export async function mergeExtractedTreeIntoDestination(params) {
         }
     };
     await walk(params.sourceDir);
+    throwIfAborted(params.signal);
 }
 export function createArchiveSymlinkTraversalError(originalPath) {
     return symlinkTraversalError(originalPath);
diff --git a/dist/archive.js b/dist/archive.js
index e678a7a..4a22e02 100644
--- a/dist/archive.js
+++ b/dist/archive.js
@@ -184,6 +184,7 @@ async function extractZip(params) {
                     sourceDir: stagingRealDir,
                     destinationDir: params.destDir,
                     destinationRealDir,
+                    signal: params.deadline.signal,
                 });
                 params.deadline.check();
             },
@@ -325,6 +326,7 @@ export async function extractArchive(params) {
                             sourceDir: stagingDir,
                             destinationDir: params.destDir,
                             destinationRealDir,
+                            signal: deadline.signal,
                         });
                         deadline.check();
                     },
