{
  "schema_version": 1,
  "iter": 0,
  "ts": "2026-09-04T03:34:03.500Z",
  "goal": "Confirm the amended goal contract against the preserved candidate, include the existing `Reflect.get` correction, and emit iter_complete so the orchestrator captures the current candidate for advisory read-only review.",
  "constraints": [
    "Read the amended goal.json completely and preserve plan.md, goal.json, coordination ledgers, all prior evidence, and the five-file implementation content.",
    "Do not rerun the full suite, baseline comparisons, aggregate static checks, build, or documentation suite already evidenced green.",
    "Do not change implementation, tests, or docs. Preserve and include the existing `Reflect.get` correction at settled-turn-finalization.unavailable.test.ts:141.",
    "Run only the minimum checks needed for the amended contract: the focused unavailable-finalizer regression, candidate/working-tree whitespace and scope union, production-versus-test/docs/coordination LOC classification, and exact file-hash comparison against the previously tested candidate.",
    "Confirm from recorded evidence that every selected broad failure class satisfies the amended exact-base equivalence rule and that no failure touches a candidate file.",
    "Do not stage or commit manually; the orchestrator owns candidate capture and commits.",
    "After these checks pass, emit iter_complete with exact file hashes and evidence so the orchestrator captures the current candidate.",
    "Reviewer remains strictly read-only and performs a defect-first review of the exact captured candidate content, including direct Codex contract, caller/callee and sibling surfaces, tests, docs, baseline exception, and LOC.",
    "Reviewer must label this first verdict advisory because coordination artifacts remain in the branch diff. It must not claim the final exact-head gate satisfied; a second review is mandatory after mechanical coordination-artifact cleanup.",
    "Do not push or create a pull request."
  ],
  "success_criteria": [
    "The focused unavailable-finalizer regression passes with `Reflect.get` present and no implementation change.",
    "Amended broad-test baseline-equivalence and Autoloop scope rules are confirmed from exact recorded evidence.",
    "Candidate scope, whitespace, hashes, and production LOC satisfy the amended contract.",
    "Coder emits iter_complete without a manual commit, enabling orchestrator candidate capture.",
    "Reviewer returns a read-only defect-first advisory verdict on the exact captured candidate and explicitly reserves final approval for the post-cleanup exact SHA."
  ],
  "max_attempts": 1
}