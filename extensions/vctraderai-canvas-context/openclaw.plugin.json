{
  "id": "vctraderai-canvas-context",
  "name": "VC Trader AI Canvas Context",
  "description": "Read-only workspace-scoped tool: Read the durable cockpit canvas document (chart drawings as data) for one chart. Calls the propfirm_manager BFF as the workspace owner (PFM_AGENT_TOKEN). READ_ONLY per ADR 0078.",
  "activation": {
    "onStartup": true
  },
  "contracts": {
    "tools": [
      "canvas_context"
    ]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
