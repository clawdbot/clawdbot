{
  "id": "vctraderai-enqueue-backtest-worker-job",
  "name": "VC Trader AI Enqueue Backtest Worker Job (Propose)",
  "description": "Stages a dispatch a backtest worker job. proposal for human review. Calls the propfirm_manager BFF at /api/v1/openclaw/stage with tool_name enqueue_backtest_worker_job. PROPOSE_ONLY per ADR 0078 - it never executes directly; the staged descriptor is reviewed + Applied in the chat.",
  "activation": {
    "onStartup": true
  },
  "contracts": {
    "tools": [
      "enqueue_backtest_worker_job"
    ]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
