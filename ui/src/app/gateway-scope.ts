// Keep legacy UI imports on the package-owned normalization contract.
export {
  gatewayCredentialScope as normalizeGatewayCredentialScope,
  gatewayOriginScope as normalizeGatewayTokenScope,
} from "@openclaw/gateway-client/browser";
